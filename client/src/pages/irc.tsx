import IRCChat from "../components/IRC/IRCChat";

const IRCPage = () => {
    return (
        <div className="h-screen  relative">
            <IRCChat />
        </div>
    );
};

export default IRCPage;
